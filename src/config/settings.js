// var env = process.env
var settings = {
  // 这里配置一些全局变量
  gbs: {
    // host: '/api'
    host: ''
  },
  baseData: {
    home: {
      // 主页大屏的默认配置
      w: 1920,
      h: 1080
    }
  }
}
module.exports = settings
